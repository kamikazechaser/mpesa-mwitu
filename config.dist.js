exports = module.exports = {
    server: {
        // Port on which the server will run on
        // defaults to 3000
        port: 3000
    },
    // MongoDB url
    db: "",
    // smsSync secret
    secret: ""
}